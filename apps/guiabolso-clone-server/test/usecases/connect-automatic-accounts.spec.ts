import { AccountData } from "@/usecases/ports"

describe('Connect automatic accounts use case', () => {
    test.skip('should create all accounts linked to an item in data provider', async () => {
        // const validItemId = 'valid-item-id'

        // const sut = new ConnectAutomaticAccounts()

        // const connectRequest = {
        //     itemId: validItemId
        // }

        // const response = (await sut.perform(connectRequest)).value as AccountData[]
        // expect(response.length).toBe(2)

    })
})